# CRITICAL: 必须修改！运行以下命令生成真实密钥：
# openssl rand -hex 32
APP_SECRET=REPLACE_THIS_RUN_OPENSSL_RAND_HEX_32

# PostgreSQL 密码
# 建议强密码，仅用于 Umami 与数据库内部通信
DB_PASSWORD=change-me-to-a-strong-password

# Umami 对外暴露的端口
UMAMI_PORT=3000
